language: python
python:
  - "2.7"
#  - "3.2"
#  - "3.3"

# command to install dependencies
install:
  - "pip install -r requirements.txt"

before_script:
  - mkdir -p shippable/codecoverage shippable/testresults

# command to run tests
script:
  - pep8 --verbose --show-pep8 sf*.py
  - pylint --rcfile=.pylintrc sf*.py
  - nosetests -v tests.py
      --with-xunit
      --xunit-file=shippable/testresults/nosetests.xml
      --with-coverage
      --cover-branches
      --cover-xml
      --cover-xml-file=shippable/codecoverage/coverage.xml